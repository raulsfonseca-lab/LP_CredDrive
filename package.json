{
  "name": "creddrive-landing-page",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page estática da CredDrive para captura de leads.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "test": "node --check script.js"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
